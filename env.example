# Environment Configuration (Example)
# Copy this file to .env and update with your values
# .env is ignored by git for security

# Base URL for resources (if using external storage)
# BASE_URL=https://your-cdn.com

# API endpoint (if you add a backend in the future)
# API_URL=https://api.example.com

# Feature flags
# ENABLE_SEARCH=true
# ENABLE_SORTING=true

# Note: This is just an example for future use
# The current version doesn't use environment variables yet
